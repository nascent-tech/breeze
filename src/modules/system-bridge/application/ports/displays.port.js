export class DisplaysPort {
  list() {
    throw new Error('DisplaysPort.list has no implementation');
  }

  onConnected(listener) {
    throw new Error('DisplaysPort.onConnected has no implementation');
  }

  onDisconnected(listener) {
    throw new Error('DisplaysPort.onDisconnected has no implementation');
  }

  onReconfigured(listener) {
    throw new Error('DisplaysPort.onReconfigured has no implementation');
  }
}
